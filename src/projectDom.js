export default class ProjectDomManipulator {
    constructor(project) {

    }
    processProject(project) {
        let nav = document.querySelector("nav")
        let div = document.createElement("div")
        div.classList.add("project")
        div.innerHTML = `
    }
}
